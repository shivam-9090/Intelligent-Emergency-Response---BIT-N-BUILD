name: Backend CI

on:
  pull_request:
    branches: [main, develop, develop-ved]
    paths:
      - "backend/**"
  push:
    branches: [develop, develop-ved]
    paths:
      - "backend/**"

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install -r requirements.txt
      - run: pytest -q
